program TestControl;
var
  x, y: integer;
  flag: boolean;
begin
  x := 10;
  y := 20;
  flag := (x < y) and (y > 0);
  if flag then
  begin
    x := x + 1;
  end
  else
  begin
    x := x - 1;
  end;
  if not flag then
  begin
    y := 0;
  end;
  writeln(x);
end.